[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "adopted-tiny-cli"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
tiny-cli = "tiny_cli:main"

[tool.setuptools]
py-modules = ["tiny_cli"]
