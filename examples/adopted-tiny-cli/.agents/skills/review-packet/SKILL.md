# Review Packet

Use `docs/review-packet-template.md` before handing work to a human.

Include issue context, PR status, summary, changed files, test results, CI status, risks, and follow-ups.

Validate with:

```sh
python -m harness.cli validate-review-packet --path docs/exec-plans/completed/<issue-id>/review-packet.md
```
