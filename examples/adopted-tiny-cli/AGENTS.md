# Agent Instructions

Start with `docs/README.md` for the project knowledge map and `WORKFLOW.md` for runtime expectations.

Work in small changes, preserve the CLI's simple standard-library style, and run `python -m unittest discover -s tests` before handoff.

Leave a review packet using `docs/review-packet-template.md`.
