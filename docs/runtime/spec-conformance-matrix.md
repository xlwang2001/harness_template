# Runtime SPEC Conformance Matrix

Status labels: `Implemented`, `Partial`, `Planned`, `Deferred`.

| SPEC area | Requirement summary | Status | Implementation/test references | Next TODO |
|---|---|---|---|---|
| Workflow/config parsing | Load `WORKFLOW.md`, split optional front matter from prompt body, apply defaults, resolve `$VAR` values, normalize paths, and validate dispatch-critical fields. | Implemented | `harness/runtime/workflow.py`; `docs/runtime/README.md`; `tests/runtime/test_workflow.py` | Revisit dependency choice only if future templates require unsupported YAML features. |
| Dynamic reload | Detect workflow changes, reload without restart, keep last known good config after invalid edits, and apply future config to dispatch behavior. | Implemented | `WorkflowReloader` in `harness/runtime/workflow.py`; `RuntimeService.run_forever`; `tests/runtime/test_workflow.py`; `tests/runtime/test_orchestrator.py` | Add filesystem watch support only if polling plus content signatures become insufficient. |
| Dispatch preflight | Validate tracker kind, tracker auth, project slug, and Codex command at startup and before each dispatch cycle while keeping reconciliation active on invalid tick config. | Implemented | `validate_dispatch_config` in `harness/runtime/workflow.py`; `Orchestrator.tick_once`; `tests/runtime/test_workflow.py`; `tests/test_cli.py`; `tests/runtime/test_orchestrator.py` | Revisit if additional dispatch-critical config fields are added. |
| Orchestrator state | Maintain claimed, running, SPEC live-session metadata, worker future, retry, completed, latest run-attempt outcome, token, runtime, and rate-limit state through one orchestrator authority, including same-worker continuation decisions up to `agent.max_turns`; optionally persist scheduler-safe retry, attempt, completed, token, rate-limit, and session metadata across restarts without restoring live process ownership. | Implemented | `harness/runtime/models.py`; `harness/runtime/orchestrator.py`; `harness/runtime/state_persistence.py`; `tests/runtime/test_orchestrator.py`; `tests/runtime/test_state_persistence.py`; `tests/runtime/test_agent.py`; `tests/runtime/test_integration_profile.py`; `docs/runtime/runbooks.md`; `docs/runtime/production-readiness-checklist.md` | Revisit only if adding a database-backed status surface or multi-process ownership. |
| Polling, scheduling, retry, reconciliation | Reconcile running work, fetch active candidates, sort by priority and age, dispatch within concurrency, process due retries including opt-in persisted retry timers, increment retried failure attempts, detect stalls, clean terminal workspaces, request cancellation when tracker state leaves active work or runtime shutdown begins, release missing refreshed states and stale retry claims, record terminal attempt reasons, and re-check tracker state between same-worker continuation turns. | Implemented | `Orchestrator.tick_once`, `process_due_retries`, `reconcile_running`, `startup_terminal_cleanup`, `shutdown`; `harness/runtime/state_persistence.py`; `tests/runtime/test_orchestrator.py`; `tests/runtime/test_state_persistence.py`; `tests/runtime/test_service.py`; `tests/runtime/test_integration_profile.py`; `docs/runtime/runbooks.md`; `docs/runtime/production-readiness-checklist.md` | Revisit if production needs durable distributed scheduling rather than local JSON state. |
| Workspace safety and hooks | Sanitize issue identifiers, keep workspace paths under root, reject non-directory workspace paths, create/reuse preserved workspaces after restart, run lifecycle hooks with timeout, run `after_run` after completed or failed attempts once a workspace exists, isolate terminal cleanup failures, and clean terminal workspaces. | Partial | `harness/runtime/workspace.py`; `tests/runtime/test_workspace.py`; `tests/runtime/test_orchestrator.py`; `tests/runtime/test_integration_profile.py`; `docs/runtime/runbooks.md`; `docs/runtime/production-readiness-checklist.md` | No immediate TODO; revisit if adding workspace population or remote checkout support. |
| Codex app-server runner | Launch configured command inside the per-issue workspace, speak through a schema-aligned stdio JSON-lines adapter, initialize session/thread/turn, stream events, reuse one live thread for same-worker continuation turns, enforce read and turn timeouts, map terminal errors, fail user-input-required turns without stalling, auto-approve command/file-change approval requests, handle registered client tools through JSON-RPC responses, reject unsupported tool calls without stalling, and keep the exact target Codex schema isolated behind `CodexAgentRunner`. | Partial | `harness/runtime/agent.py`; `tests/runtime/test_agent.py`; `tests/runtime/test_codex_schema.py`; `tests/runtime/test_integration_profile.py`; `tests/test_cli.py`; `docs/runtime/README.md`; `docs/runtime/production-readiness-checklist.md` | Future extension backlog tracks schema-backed client-tool advertisement if supported later. |
| Linear tracker integration | Fetch candidate issues, terminal-state issues, and issue states using configured project/state filters; normalize issue fields, labels, blockers, pagination, error categories, expose explicit Linear write APIs for comments, state transitions, and pull-request metadata when workflow code owns those writes, and provide opt-in read-only real Linear smoke tests. | Partial | `harness/runtime/tracker.py`; `tests/runtime/test_tracker.py`; `tests/runtime/test_integration_profile.py`; `docs/runtime/README.md`; `docs/runtime/production-readiness-checklist.md` | Future extension backlog tracks non-Linear adapters. |
| Prompt rendering | Render strict dotted `issue` and `attempt` variables and fail on unknown variables, unknown filters, malformed interpolation, and unsupported template expressions. | Implemented | `harness/runtime/prompt.py`; `tests/runtime/test_workflow.py` | Add richer template support only if runtime adopts a real template engine. |
| Logging/observability | Surface runtime startup, reload, validation, dispatch, retry, reconciliation, hook, snapshot, session events with session IDs when known, token totals, live runtime seconds, latest rate-limit payloads, approval auto-resolution, client-tool outcomes, optional dashboard state, status-surface failures, and workflow-configurable console/file log sinks to operators. | Implemented | `harness/runtime/runtime_logging.py`; `RuntimeService` logging in `harness/runtime/service.py`; `tests/runtime/test_logging.py`; `tests/runtime/test_orchestrator.py`; `tests/runtime/test_agent.py`; `tests/runtime/test_http.py`; `tests/runtime/test_service.py`; `docs/runtime/runbooks.md` | Revisit only if adding remote log sinks or a richer dashboard. |
| Failure recovery | Keep service alive on reload errors, retry worker failures with backoff, optionally restore safe retry/session metadata from local JSON, release claims when retries disappear, tolerate startup cleanup fetch and per-issue cleanup failures, tolerate corrupt or unwritable runtime state files, shutdown cleanly on interrupts/signals, recover from restart using tracker and filesystem state without restoring live ownership, and continue when a configured file log sink fails. | Implemented | `WorkflowReloader`; `Orchestrator.schedule_retry`; `RuntimeStateStore`; `RuntimeService.run_forever`; `startup_terminal_cleanup`; `tests/runtime/test_orchestrator.py`; `tests/runtime/test_state_persistence.py`; `tests/runtime/test_service.py`; `tests/runtime/test_integration_profile.py`; `docs/runtime/production-readiness-checklist.md` | Revisit only for database-backed recovery, external supervisor integration, or multi-process leasing. |
| Optional HTTP/API | Expose optional loopback `/` dashboard, `GET /api/v1/state`, `GET /api/v1/<issue_identifier>`, and `POST /api/v1/refresh` endpoints with JSON error envelopes, unsupported method handling, refresh coalescing, `server.port` enablement, and CLI `--port` override. | Implemented | `harness/runtime/status_server.py`; `harness/runtime/service.py`; `harness/cli.py`; `tests/runtime/test_http.py`; `tests/runtime/test_service.py`; `tests/test_cli.py`; `docs/runtime/runbooks.md` | Revisit only if HTTP API version changes. |
| Optional `linear_graphql` tool | Expose a constrained client-side Linear GraphQL query/mutation tool that reuses configured Linear auth, accepts raw query strings or `{query, variables}`, rejects invalid input, subscriptions, and multiple operations, preserves GraphQL error bodies, and returns structured failure payloads for missing auth or transport failures. Runtime-owned tools are handled when the app-server requests them. Startup advertisement is intentionally not sent because the current targeted generated Codex schema has no stable client-tool advertisement field. | Partial | `harness/runtime/client_tools.py`; `harness/runtime/service.py`; `tests/runtime/test_tracker.py`; `tests/runtime/test_agent.py`; `tests/runtime/test_codex_schema.py`; `docs/runtime/runbooks.md` | Future extension backlog tracks schema-backed startup advertisement if supported later. |
| Targeted Codex schema verification | Verify the stdio protocol envelopes emitted by `CodexAgentRunner` against the generated schema for the installed Codex app-server version. | Implemented | `tests/runtime/test_codex_schema.py`; `Makefile`; `pyproject.toml` | Run `make codex-schema-test` during Codex app-server upgrades; default CI keeps this gated. |
| Real integration profile | Provide opt-in integration checks for service-loop restart and cleanup behavior, read-only real Linear coverage, optional real Codex command coverage, and isolated workspaces, skipped by default unless `HARNESS_RUN_INTEGRATION=1` is set. | Implemented | `tests/runtime/test_integration_profile.py`; `Makefile`; `docs/runtime/README.md` | Run before production use with real credentials and targeted Codex command. |
