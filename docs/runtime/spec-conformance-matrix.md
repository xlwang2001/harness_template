# Runtime SPEC Conformance Matrix

Status labels: `Implemented`, `Partial`, `Planned`, `Deferred`.

| SPEC area | Requirement summary | Status | Implementation/test references | Next TODO |
|---|---|---|---|---|
| Workflow/config parsing | Load `WORKFLOW.md`, split optional front matter from prompt body, apply defaults, resolve `$VAR` values, normalize paths, and validate dispatch-critical fields. | Implemented | `harness/runtime/workflow.py`; `docs/runtime/README.md`; `tests/runtime/test_workflow.py` | Revisit dependency choice only if future templates require unsupported YAML features. |
| Dynamic reload | Detect workflow changes, reload without restart, keep last known good config after invalid edits, and apply future config to dispatch behavior. | Implemented | `WorkflowReloader` in `harness/runtime/workflow.py`; `RuntimeService.run_forever`; `tests/runtime/test_workflow.py`; `tests/runtime/test_orchestrator.py` | Add filesystem watch support only if polling plus content signatures become insufficient. |
| Dispatch preflight | Validate tracker kind, tracker auth, project slug, and Codex command at startup and before each dispatch cycle while keeping reconciliation active on invalid tick config. | Implemented | `validate_dispatch_config` in `harness/runtime/workflow.py`; `Orchestrator.tick_once`; `tests/runtime/test_workflow.py`; `tests/test_cli.py`; `tests/runtime/test_orchestrator.py` | Revisit if additional dispatch-critical config fields are added. |
| Orchestrator state | Maintain claimed, running, SPEC live-session metadata, worker future, retry, completed, latest run-attempt outcome, token, runtime, and rate-limit state through one orchestrator authority. | Partial | `harness/runtime/models.py`; `harness/runtime/orchestrator.py`; `tests/runtime/test_orchestrator.py`; `tests/runtime/test_agent.py` | Implement same-worker same-thread continuation turns up to `agent.max_turns`. |
| Polling, scheduling, retry, reconciliation | Reconcile running work, fetch active candidates, sort by priority and age, dispatch within concurrency, process due retries, increment retried failure attempts, detect stalls, clean terminal workspaces, request cancellation when tracker state leaves active work or runtime shutdown begins, and record terminal attempt reasons for success, failure, timeout, stall, and reconciliation cancellation. | Partial | `Orchestrator.tick_once`, `process_due_retries`, `reconcile_running`, `startup_terminal_cleanup`, `shutdown`; `tests/runtime/test_orchestrator.py`; `tests/runtime/test_service.py` | Implement same-worker same-thread continuation turns up to `agent.max_turns`. |
| Workspace safety and hooks | Sanitize issue identifiers, keep workspace paths under root, reject non-directory workspace paths, create/reuse workspaces, run lifecycle hooks with timeout, run `after_run` after completed or failed attempts once a workspace exists, and clean terminal workspaces. | Partial | `harness/runtime/workspace.py`; `tests/runtime/test_workspace.py`; `tests/runtime/test_orchestrator.py` | Add local runbooks for workspace cleanup, hook failures, and failure modes. |
| Codex app-server runner | Launch configured command inside the per-issue workspace, speak through a stdio JSON-lines protocol adapter, initialize session/thread/turn, stream events, enforce read and turn timeouts, map terminal errors, fail user-input-required turns without stalling, auto-approve command/file-change approval requests, handle registered client tools, reject unsupported tool calls without stalling, and keep the exact target Codex schema isolated behind `CodexAgentRunner`. | Partial | `harness/runtime/agent.py`; `tests/runtime/test_agent.py`; `tests/test_cli.py`; `docs/runtime/README.md` | Implement same-worker same-thread continuation turns, then verify envelopes against the targeted Codex generated schema. |
| Linear tracker integration | Fetch candidate issues, terminal-state issues, and issue states; normalize issue fields, labels, blockers, pagination, error categories, and provide an opt-in real Linear smoke test. | Partial | `harness/runtime/tracker.py`; `tests/runtime/test_tracker.py`; `tests/runtime/test_integration_profile.py` | Add broader restart/cleanup tests and operator runbooks. |
| Prompt rendering | Render strict dotted `issue` and `attempt` variables and fail on unknown variables, unknown filters, malformed interpolation, and unsupported template expressions. | Implemented | `harness/runtime/prompt.py`; `tests/runtime/test_workflow.py` | Add richer template support only if runtime adopts a real template engine. |
| Logging/observability | Surface runtime startup, reload, validation, dispatch, retry, reconciliation, hook, snapshot, session events with session IDs when known, token totals, live runtime seconds, latest rate-limit payloads, approval auto-resolution, client-tool outcomes, optional dashboard state, and status-surface failures to operators. | Partial | `harness/runtime/runtime_logging.py`; `RuntimeService` logging in `harness/runtime/service.py`; `tests/runtime/test_logging.py`; `tests/runtime/test_orchestrator.py`; `tests/runtime/test_agent.py`; `tests/runtime/test_http.py` | Add local runbooks for workspace cleanup, logs, failure modes, dashboard/API operation, and SPEC compatibility upgrades. |
| Failure recovery | Keep service alive on reload errors, retry worker failures with backoff, release claims when retries disappear, tolerate startup cleanup fetch failures, shutdown cleanly on interrupts/signals, and recover from restart using tracker and filesystem state. | Partial | `WorkflowReloader`; `Orchestrator.schedule_retry`; `RuntimeService.run_forever`; `startup_terminal_cleanup`; `tests/runtime/test_orchestrator.py`; `tests/runtime/test_service.py` | Add broader restart/cleanup tests and optional integration coverage. |
| Optional HTTP/API | Expose optional loopback `/` dashboard, `GET /api/v1/state`, `GET /api/v1/<issue_identifier>`, and `POST /api/v1/refresh` endpoints with JSON error envelopes, unsupported method handling, refresh coalescing, `server.port` enablement, and CLI `--port` override. | Implemented | `harness/runtime/status_server.py`; `harness/runtime/service.py`; `harness/cli.py`; `tests/runtime/test_http.py`; `tests/runtime/test_service.py`; `tests/test_cli.py` | Add dashboard/API operation runbook. |
| Optional `linear_graphql` tool | Expose a constrained client-side Linear GraphQL query/mutation tool that reuses configured Linear auth, accepts raw query strings or `{query, variables}`, rejects invalid input, subscriptions, and multiple operations, preserves GraphQL error bodies, and returns structured failure payloads for missing auth or transport failures. | Implemented | `harness/runtime/client_tools.py`; `harness/runtime/service.py`; `tests/runtime/test_tracker.py`; `tests/runtime/test_agent.py` | Add operator guidance for safe tool use in runbooks. |
| Targeted Codex schema verification | Verify the stdio protocol envelopes emitted by `CodexAgentRunner` against the generated schema for the installed Codex app-server version. | Planned | None yet | Add schema-gated protocol compatibility tests once the targeted Codex schema is available locally. |
| Real integration profile | Provide opt-in integration checks for real Linear/Codex credentials and isolated workspaces, skipped by default unless `HARNESS_RUN_INTEGRATION=1` is set. | Implemented | `tests/runtime/test_integration_profile.py`; `Makefile`; `docs/runtime/README.md` | Run before production use with real credentials and targeted Codex command. |
