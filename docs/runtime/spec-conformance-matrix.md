# Runtime SPEC Conformance Matrix

| SPEC area | Requirement summary | Status | Implementation/test references | Next TODO |
|---|---|---|---|---|
| Workflow/config parsing | Load `WORKFLOW.md`, split optional front matter from prompt body, apply defaults, resolve `$VAR` values, normalize paths, and validate dispatch-critical fields. | Implemented | `harness/runtime/workflow.py`; `docs/runtime/README.md`; `tests/runtime/test_workflow.py` | Revisit dependency choice only if future templates require unsupported YAML features. |
| Dynamic reload | Detect workflow changes, reload without restart, keep last known good config after invalid edits, and apply future config to dispatch behavior. | Implemented | `WorkflowReloader` in `harness/runtime/workflow.py`; `RuntimeService.run_forever`; `tests/runtime/test_workflow.py`; `tests/runtime/test_orchestrator.py` | Add filesystem watch support only if polling plus content signatures become insufficient. |
| Dispatch preflight | Validate tracker kind, tracker auth, project slug, and Codex command before dispatch. | Implemented | `validate_dispatch_config` in `harness/runtime/workflow.py`; `tests/runtime/test_workflow.py`; `tests/test_cli.py` | Add defensive per-tick validation coverage for active reconciliation when config reload fails. |
| Orchestrator state | Maintain claimed, running, retry, completed, token, runtime, and rate-limit state through one orchestrator authority. | Partial | `harness/runtime/models.py`; `harness/runtime/orchestrator.py`; `tests/runtime/test_orchestrator.py` | Track worker futures in runtime state so reconciliation can cancel or mark active runs deterministically. |
| Polling, scheduling, retry, reconciliation | Reconcile running work, fetch active candidates, sort by priority and age, dispatch within concurrency, process due retries, detect stalls, and clean terminal workspaces. | Partial | `Orchestrator.tick_once`, `process_due_retries`, `reconcile_running`; `tests/runtime/test_orchestrator.py` | Add retry backoff cap, continuation retry, non-active state, and startup cleanup tests. |
| Workspace safety and hooks | Sanitize issue identifiers, keep workspace paths under root, create/reuse workspaces, run lifecycle hooks with timeout, and clean terminal workspaces. | Partial | `harness/runtime/workspace.py`; `tests/runtime/test_workspace.py`; `tests/runtime/test_orchestrator.py` | Add hook timeout tests and tests for existing non-directory workspace paths. |
| Codex app-server runner | Launch configured Codex command only inside the per-issue workspace and isolate protocol work behind a runner boundary. | Partial | `harness/runtime/agent.py`; `tests/test_cli.py`; `docs/runtime/README.md` | Implement a real Codex app-server protocol client behind `CodexAgentRunner`. |
| Linear tracker integration | Fetch candidate issues, terminal-state issues, and issue states; normalize issue fields, labels, blockers, pagination, and error categories. | Partial | `harness/runtime/tracker.py`; `tests/runtime/test_tracker.py` | Add blocker normalization tests for multiple Linear inverse relation shapes. |
| Prompt rendering | Render strict `issue` and `attempt` variables and fail on unknown template variables. | Implemented | `harness/runtime/prompt.py`; `tests/runtime/test_workflow.py` | Add stricter template parse/error surface if additional filters are introduced. |
| Logging/observability | Surface runtime startup, reload, validation, dispatch, retry, reconciliation, hook, and session events to operators. | Planned | `RuntimeService` logging in `harness/runtime/service.py`; `docs/runtime/README.md` | Add structured log records and redaction tests for secrets. |
| Failure recovery | Keep service alive on reload errors, retry worker failures with backoff, release claims when retries disappear, and recover from restart using tracker and filesystem state. | Partial | `WorkflowReloader`; `Orchestrator.schedule_retry`; `startup_terminal_cleanup`; `tests/runtime/test_orchestrator.py` | Add runtime shutdown handling and broader restart/cleanup tests. |
| Optional HTTP/API | Expose optional `/api/v1/state`, issue detail, and refresh endpoints. | Planned | None yet | Add optional HTTP status API for `/api/v1/state`, issue detail, and refresh. |
| Optional `linear_graphql` tool | Expose a constrained client-side Linear GraphQL tool to the Codex app-server session. | Planned | None yet | Add optional `linear_graphql` client-side tool extension and tests. |
| Real integration profile | Provide opt-in integration checks for real Linear/Codex credentials and isolated workspaces. | Deferred | None yet | Add real integration test profile gated by explicit environment variables. |
